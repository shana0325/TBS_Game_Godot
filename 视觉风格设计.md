# TBS Game Godot 视觉风格规范

## 目标风格

- 战斗小人使用清晰的像素风，优先保证格子内轮廓、阵营和行动状态可辨认。
- 角色立绘、技能图标、遗物图标和界面使用统一的二次元暗色幻想风。
- 主色为深蓝紫，强调色为暖金；危险、伤害和不可用状态分别使用红色、亮红和低饱和灰紫。
- 界面首先保证信息清晰，再添加装饰。装饰不得压低文字对比度或遮挡交互区域。

## 当前界面结构

- 部署和战斗共用战场尺寸与底部单位栏，切换场景时不改变棋盘视觉尺度。
- 角色详情采用左侧章节导航、中间大立绘、右侧连续滚动内容。
- 技能详情、遗物详情、背包和单位详情均使用模态遮罩，禁止鼠标穿透。
- 爬塔奖励在战斗场景上方显示，奖励内容可隐藏，开关位于独立顶层画布。
- 顶部遗物区显示图标与叠加层数，点击打开详情。

## 组件规则

### 面板

- 深色半透明底，紫灰描边，圆角保持克制。
- 模态面板必须覆盖输入遮罩，并支持 ESC 或鼠标右键关闭。
- 首次显示和窗口变化后都要按视口重新计算尺寸与位置。

### 按钮

- 默认、悬停、按下、禁用状态必须能直接区分。
- 主要操作使用暖金强调，普通操作使用紫灰色。
- 同一操作区的按钮保持一致高度、间距和文字格式。

### 图标与数值

- 技能和遗物图标使用正方形构图；战斗小人保持像素采样。
- 可叠加遗物只显示一个图标，并在旁边显示 `×层数`。
- 血条以最大生命为刻度；护盾条同样以最大生命为刻度，超过最大生命后保持满条。
- 角色基础属性显示当前生命、当前护盾与护盾上限。

## 素材目录

- 正式运行素材：`assets/units/`、`assets/skills/`、`assets/relics/`、`assets/fonts/`。
- `assets/reference/ui_material/` 仅保存候选参考素材，未确认接入前不得被场景或主题引用。
- 新素材接入后必须检查 Godot 导入、实际场景显示和旧路径引用。

## 验收重点

- 1280×720 与 1920×1080 下不溢出、不重叠。
- 弹窗首次打开与重复打开的尺寸一致。
- 弹窗关闭、按钮点击和滚动不会穿透到底层单位。
- 部署预览和正式战斗中的单位属性一致。
- 内容较长时使用滚动，不通过缩小字体强行塞入。
