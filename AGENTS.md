# TBS Game Godot Agent 维护规则

本文件只适用于当前项目根目录 `TBS_Game_Godot`。它用于让后续 Agent 在有限上下文内稳定维护项目，不替代游戏设计文档，也不要求每次读取整个仓库。

## 项目概况

- Godot 4.7 / GDScript。
- 2D 自走棋式战棋与爬塔原型。
- 数据主要来自 `data/` 下的 JSON，运行流程由场景、屏幕控制器、核心会话和战斗模块协作完成。
- 结构索引见 [docs/PROJECT_MAP.md](docs/PROJECT_MAP.md)。开始任务前先读本文件和该索引。

## 信息优先级

判断“当前实际行为”时，以当前代码和数据为准；判断“应该做成什么”时，以用户最新明确要求为最高优先级，其次参考相关设计文档。`PROJECT_MAP.md` 只负责导航，旧聊天记录只作为背景，不作为未经确认的实现要求。

不要因为代码与设计文档不一致就自动改动其中一方。先确认当前任务是在修正实现、更新设计，还是两者都需要调整。

## 上下文读取策略

1. 先读本文件和 `docs/PROJECT_MAP.md`。
2. 根据任务只读取相关设计文档、场景、脚本和数据文件。
3. 用 `rg` 定位引用关系，再打开必要的上下文；不要默认全文扫描项目。
4. 不默认读取 `.godot/`、`.import`、`.uid`、`web_build/` 和生成日志。
5. LC2 仅按 `docs/LC2参考索引.md` 使用：除非用户明确要求参考某个具体功能，否则不读取、运行或修复 `D:\Shana Program\文档\lc2`。
6. 已经确认过且没有变化的目录，不重复读取；只有任务范围变化或证据不足时才扩大范围。

## 修改原则

- 以最小改动完成任务，保留用户已有的未提交修改和当前逻辑。
- 涉及 UI 时，优先复用已有场景、组件、状态接口和文本格式，避免为同一行为创建第二套实现。
- `scripts/core/`、`scripts/battle/` 保持与具体 UI 场景解耦；UI 通过 `GameSession`、`BattleManager` 等公开状态/事件接口获取信息。
- 可调整的单位、技能、Buff、装备和关卡数值优先放在 `data/`，不要散落在 UI 脚本中。
- 正式运行时资源放在 `assets/` 对应子目录；`assets/reference/ui_material/` 是未接入的候选素材库，不得无意中加入场景或主题引用。
- 移动或重命名文件后，必须检查旧路径引用，并同步维护 `README.md`、`docs/PROJECT_MAP.md` 或相关说明。
- 每个 GDScript 模块开头应有简短中文注释说明作用。新增 Python 文件时，文件开头必须有简短中文注释说明作用；当前项目尽量不为简单任务新增 Python。
- 文本文件使用 `apply_patch` 修改；不要用脚本或 shell 重写大段文件来规避审阅。
- 不手动修改 Godot 生成的 `.import`、`.uid` 和 `.godot/` 内容，除非明确是在处理导入/缓存故障且已先确认原因。
- 不使用 `git reset --hard`、`git clean` 或未获明确授权的强制覆盖操作。

## 推荐工作流

1. 明确任务属于设计、数据、战斗、流程、UI、资源还是发布。
2. 从 `PROJECT_MAP.md` 找到最小相关文件集合。
3. 先检查现有实现、引用和工作区状态，再设计最小变更。
4. 修改后执行与风险匹配的验证，并检查是否产生旧路径、重复逻辑或 UI/逻辑耦合。
5. 只有在结构、设计或交接信息确实变化时才更新对应文档，不为每个小修复重写文档。
6. 汇报修改文件、验证结果和仍存在的限制；GitHub 推送、提交或分支操作必须有用户明确要求。

## 验证基线

涉及脚本、场景或数据时，优先执行 Godot 4.7 无头编辑器扫描：

```powershell
& 'D:\Shana Program\Godot\Godot_v4.7.1-stable_win64_console.exe' --headless --path '<项目路径>' --editor --quit
```

随后按任务范围运行相关场景或测试，并执行：

```powershell
git diff --check
git status --short
```

不要把“无头扫描通过”误认为完整的交互验证；部署拖拽、单位信息卡、奖励悬浮窗等交互改动仍需要实际运行场景检查。

## 文档职责

- `README.md`：给项目使用者看的概览、运行方式和入口。
- `docs/PROJECT_MAP.md`：给 Agent 和开发者看的结构导航，只保留稳定的目录/模块关系。
- `docs/HANDOFF.md`：阶段性进度、已知问题、恢复步骤和下一步工作。
- 设计文档：记录玩法和数值意图，不用于替代代码检查。
- `docs/LC2参考索引.md`：限制外部参考项目的读取范围，避免无关源码进入上下文。

如果后续新增核心目录、改变逻辑/UI边界或再次整理资源目录，先更新 `PROJECT_MAP.md`，再在本文件中补充长期有效的维护规则。
